---
schema_version: "2"
change_id: "dependabot-audit-recovery"
created_at: "2026-09-14T16:44:58+08:00"
title: "Restore the production dependency audit baseline"
change_kind: "dependency"
implementation_status: "verified"
review_status: "pending"
project_root: "/Users/wuqisen/dev/Praxis"
parents: ["issue-751-post-deploy-relay-smoke"]
supersedes: []
scopes: ["project", "dependency-security", "architecture-model"]
changed_files: ["package-lock.json", "docs/architecture/impact-log.md"]
excluded_preexisting_files: [".claude", "docs/CLAUDE_EXPERIMENTAL_CAPABILITIES.md", "docs/research"]
base_revision: "git:6bfefda60c37215805201d2822b41b900ea97ec2"
observed_revision: "worktree:fix/dependabot-audit-recovery@6bfefda60c37215805201d2822b41b900ea97ec2"
architecture_verdict: "NO_MODEL_CHANGE"
architecture_evidence: "The dependency impact entry records zero bounded-source drift, no changed boundary or critical flow, affected IDs none, a valid canonical model, and two byte-identical ten-view renders matching canonical output."
risk: "high"
requirement_ids: ["R1", "R2", "R3", "R4", "R5"]
repair_of: []
---

# Executive Summary
Praxis now resolves the inherited production dependency advisories by advancing the installed Hono resolution from 4.12.34 to 4.13.7 and Sharp from 0.35.3 to 0.35.4, including Sharp-owned optional platform, libvips, and WASM packages. Direct dependency ranges, runtime source, public interfaces, persisted data, and Issue #763 remain unchanged. The production audit reports zero vulnerabilities.

# Review Contract
Review exactly `package-lock.json` and `docs/architecture/impact-log.md` as the coherent security-baseline repair. Confirm every changed lockfile package object belongs to Hono or the Sharp dependency closure, `package.json` is unchanged, fresh installation is reproducible, Sharp image and MCP paths work, full/package/performance gates pass, and the audit is clean. Confirm no Issue #763 file, runtime source, test, configuration, dependency range, migration, release, or provider behavior entered this change.

# Before And After
At the base revision, the installed Hono 4.12.34 and Sharp 0.35.3 resolutions triggered current moderate and high production advisories, blocking Issue #763's mandatory audit gate. After this change, the same declared dependency ranges resolve Hono 4.13.7, Sharp 0.35.4, and compatible Sharp-owned optional packages; `npm audit --omit=dev` exits successfully with zero vulnerabilities.

# Implementation Path
The primary session isolated the inherited audit failure from Issue #763, fixed the target versions and high-risk gates, and dispatched `/root/dependabot_audit_recovery` to the required `gpt-5.6-luna` implementation model. The implementation task reached terminal `done`. It used the bounded npm lockfile update, performed a fresh install, and ran the required verification batch. Independent Standards and Spec reviews passed, and the primary session accepted the exact two-file product change after object-level lockfile analysis and architecture assessment.

# Change Surface
`package-lock.json` changes 28 package objects, all within the Hono or Sharp resolution families: Hono, Sharp, Sharp platform binaries, libvips packages, and the Sharp WASM dependency closure. `docs/architecture/impact-log.md` records the accepted `NO_MODEL_CHANGE` assessment. `package.json`, source, tests, CI, release metadata, provider adapters, persistence, and qualification files have no diff. Review Ledger storage generated by recording this capsule is governance state, not an additional dependency behavior change.

# Contracts And Compatibility
All direct semver ranges in `package.json` remain exact to the base revision. The MCP SDK identity and every Sharp/MCP caller remain unchanged. A fresh `npm ci` reproduces the lockfile without tracked drift, the native package remains `praxis-agent-0.70.2`, and there is no schema, transcript, native data-plane, CLI, provider, configuration, migration, or deployment compatibility change.

# Architecture Impact
The verdict is `NO_MODEL_CHANGE`. Bounded collection reported zero added, modified, or deleted paths across the established source/model include scope because only the lockfile and impact log changed. Object-level inspection confined all dependency movement to the Hono/Sharp closure. The canonical model validated, two temporary renders produced ten byte-identical views matching canonical output, affected architecture IDs are none, and the existing source checkpoint required no update.

# Verification Evidence
Fresh `npm ci` passed without tracked drift. Focused Sharp-backed image and MCP server tests passed 50 of 50. The final `npm run check` passed 257 test files and 3,393 tests. `npm run test:package` passed for the native package, and `npm run test:performance` passed projection and quiet-frame gates, including intentional injected-regression rejection. `npm audit --omit=dev` reports zero vulnerabilities. An initial `/copy` async test failure was classified as transient only after the exact focused test passed three consecutive reruns and the complete check then passed; no production repair or waiver was introduced. Scope and diff checks pass.

# Risks And Known Gaps
Risk is high because transitive production packages and platform-specific Sharp artifacts change. The narrow lockfile closure, fresh install, focused native paths, complete suite, package, performance, audit, architecture, and rollback evidence mitigate that risk. Platform artifacts not executable on the current host are verified through deterministic lockfile integrity and the upstream package graph rather than runtime execution on every operating system and CPU. Human Review Ledger status and protected PR CI remain pending; no release or deployment authority is implied.

# Lineage And Freshness
This capsule directly descends from `issue-751-post-deploy-relay-smoke`, advances the current project and architecture-model lineage, and introduces the dependency-security scope. It fingerprints only the accepted lockfile and architecture-impact paths in the dedicated linked worktree. Pre-existing `.claude`, `docs/CLAUDE_EXPERIMENTAL_CAPABILITIES.md`, `docs/research`, ignored `.agent/**` operations, credentials, Issue #763 work, build/package output, and external relay state remain excluded and unattributed. Human review remains pending.

# Reviewer Checklist
- Confirm the lockfile resolves Hono 4.13.7 and Sharp 0.35.4 with matching Sharp platform, libvips, and WASM dependencies.
- Confirm all 28 changed package objects belong to the Hono/Sharp closure and no unrelated package version or direct dependency range changed.
- Confirm fresh install, 50 focused Sharp/MCP tests, the 3,393-test full check, package, performance, and zero-vulnerability audit evidence.
- Confirm the initial async `/copy` failure was followed by three focused passes and a complete-suite pass without a production-code change or gate waiver.
- Confirm Issue #763, runtime source, tests, configuration, public contracts, persistence, providers, migrations, and release metadata remain outside this change.
- Confirm the `NO_MODEL_CHANGE` verdict, single-commit rollback boundary, exact two-file attribution, protected local exclusions, pending human review, and pending protected PR CI.
